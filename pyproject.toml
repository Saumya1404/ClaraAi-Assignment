[project]
name = "claraai-assignment"
version = "0.1.0"
description = "Clara AI intern assignment — zero-cost automation pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faster-whisper>=1.1.0",
]

[project.scripts]
transcribe = "scripts.transcribe:main"
